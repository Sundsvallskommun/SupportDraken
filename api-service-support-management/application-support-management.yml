spring:
  datasource:
    url: jdbc:mariadb://mariadb:3306/support_management
    username: support_management
    password: support_management
  jpa:
    hibernate:
    ddl-auto: validate
  flyway:
    enabled: true

  security:
    oauth2:
      client:
        provider:
          accessmapper:
            token-uri: http://wiremock:8080/token
          emailreader:
            token-uri: http://wiremock:8080/token
          employee:
            token-uri: http://wiremock:8080/token
          eventlog:
            token-uri: http://wiremock:8080/token
          messaging:
            token-uri: http://wiremock:8080/token
          messageexchange:
            token-uri: http://wiremock:8080/token
          messaging-settings:
            token-uri: http://wiremock:8080/token
          notes:
            token-uri: http://wiremock:8080/token
          web-message-collector:
            token-uri: http://wiremock:8080/token
          citizen:
            token-uri: http://wiremock:8080/token
          relation:
            token-uri: http://wiremock:8080/token
        registration:
          accessmapper:
            client-id: the-client-id
            client-secret: the-client-secret
          emailreader:
            client-id: the-client-id
            client-secret: the-client-secret
          employee:
            client-id: the-client-id
            client-secret: the-client-secret
          eventlog:
            client-id: the-client-id
            client-secret: the-client-secret
          messaging:
            client-id: the-client-id
            client-secret: the-client-secret
          messageexchange:
            client-id: the-client-id
            client-secret: the-client-secret
          messaging-settings:
            client-id: the-client-id
            client-secret: the-client-secret
          notes:
            client-id: the-client-id 
            client-secret: the-client-secret
          web-message-collector:
            client-id: the-client-id
            client-secret: the-client-secret
          citizen:
            client-id: the-client-id
            client-secret: the-client-secret
          relation:
            client-id: the-client-id
            client-secret: the-client-secret

integration:
  accessmapper:
    url: http://accessmapper-api:8080/
  emailreader:
    url: http://emailreader-api:8080/
  employee:
    url: http://employee-api:8080/
  eventlog:
    url: http://eventlog-api:8080/
  messaging:
    url: http://messaging-api:8080/
  messageexchange:
    url: http://messageexchange-api:8080/
  messaging-settings:
    url: http://messaging-settings-api:8080/
  notes:
    url: http://notes-api:8080/
  web-message-collector:
    url: http://messaging-api:8080/
  citizen:
    url: http://citizen-api:8080/
  relation:
    url: http://relations-api:8080/

scheduler:
  emailreader:
    cron: "-"
  web-message-collector:
    cron: "-"
  notification:
    cron: "-"
  messageexchange:
    cron: "-"
    enabled: false